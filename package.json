{
  "name": "webdev-starter-BulmaUI",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.14.0",
    "nuxt-buefy": "^0.3.31"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
